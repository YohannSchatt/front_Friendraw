{
  "name": "friendraw",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5500",
    "build": "vite build",
    "preview": "vite preview",
    "start": "dotenv -e .env npx serve ."
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "vite": "^5.2.0"
  }
}
